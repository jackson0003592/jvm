概述：栈时运行时的单位，堆是存储单位

线程私有的
内部保存的栈帧 -> 一个栈帧对应一个Java方法
栈是一种快速有效的分配存储方式，访问速度仅次于程序计数器
栈不存在垃圾回收问题
-Xss 设置栈的大小

栈帧：局部变量表、操作数栈、动态链接、方法返回地址、附加
局部便量表(Local variable)：构造方法、实例方法第一个slot(index 0)是this；double、long占据2个slot

操作数栈(Operand Stack):
如果被调用的方法带有返回值的话，其返回值将会被压入当前栈帧的操作数栈中，并更新PC寄存器中下一条需要执行的字节码指令。